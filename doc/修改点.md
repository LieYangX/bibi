2026-07-06
1.记账页去掉，讲记账改成弹窗的方式，在全局又下角一个浮动按钮，点击后弹出记账弹窗
2.统计改成首页
3.流水改成明细
4.分类、预算、导入 统一放到新的页面 个人页中，新增个人页，加上关于弹出信息
5.卡片风格都去掉阴影，使用扁平设计风格
弹窗使用ui库的弹窗组件

2026-07-07
一、优化功能
1、账户限制名称唯一，加默认账户tag
账户限制名称不能重复，并且新增默认账户标识，在记一笔时，默认账户会自动选中，账户可以修改，调整默认账户和金额名称，新增账户备注字段

2、明细 增加账户、分类
明细页中，每一个交易记录，都有显示账户、分类、金额、备注等字段

二、画面兼容
1、首页中的组件，调整换行临界值
首页几个统计卡片，不要有阴影，做成响应式的布局，在不同屏幕尺寸下都能正常显示

2、明细中日期筛选内容显示换行，调整组件宽度
明细页中的日期筛选组件，调整宽度，使在不同屏幕尺寸下都能正常显示，限制是因为在不同设备上分辨率不同，时间太长超出设置的宽度导致换行了

导航栏首页一直是选中状态修复一下

## 金额显示的地方加上小眼睛

实现方案：
新增一张系统设置表，专门用来存储系统设置，比如是否显示金额脱敏眼睛，默认是显示的，key value 的格式，key 是金额脱敏，value 是是否
在金额显示的地方，根据系统设置表中的值，判断是否脱敏，是则显示小眼睛，不显示则不显示小眼睛
是脱敏的状态，默认是显示的，用户可以点击小眼睛，切换显示状态，小眼睛只控制当前组件的显示状态，不会影响其他组件的显示状态，
这个设置开关放到设置页中，范围控制在主页和账户，还有明细页

## 信用账户的加一个还款按钮

实现方案：
在账户页，新增一个还款按钮，点击后弹出还款弹窗，选择还款账户，输入还款金额，记录明细。

## 明细页增加账户筛选条件

实现方案：
在明细页，新增一个账户筛选条件，用户可以根据账户名称筛选交易记录

## 字体等优化

全局使用 MiSans 字体，禁止用户复制

## 修复问题

主页的统计图的金额不要脱敏，直接显示，用户可以直接查看金额，小眼睛只管上面四个卡片，并且四个卡片的金额要自适应显示不要换行，￥123456.789 从 ￥后换行了。
明细页的金额不要每个都显示小眼睛，小眼睛只需要在一个位置，来切换整个页面的金额显示状态

## 2026-07-14

1.小笔ai进入页面不会自动加载mcp状态，导致工具数量显示错误，打开工具页面加载完状态数量才显示正常
2.需要集成ai sdk devtools 依赖，用于开发调试和监控ai的运行状态
3.本地记忆功能缺失，新增文件读取写入工具，用于存储和读取ai的本地记忆，保存文件为md格式, 记忆分为两类，一个时用户画像，
一个是灵魂，用户画像记录用户的基本信息，灵魂记录用户的情感和行为，灵魂使用自动更新，用户发送10条消息自动使用ai提炼一次，这个条数可以通过设置也设置，叫记忆提炼阈值
并且带上灵魂的上下文，保存最新的灵魂，用户画像由ai自行决定使用工具进行更新，每次请求都带上最新的记忆灵魂。
4.skill技能全量加载到的上下文，需要动态植入，放到上下文的为skill名字和描述，由ai决定使用什么skill，通过获取skill工具返回完整的skill内容
5.ai上下文使用滑动窗口的方式，每次只加载最近的上下文，跟记忆提炼阈值保持一样，达到记忆的连续性

### 2026-07-18

1.全新版本，以ai中心化为主体，记账功能为辅助功能，UI改版。
2.新增流水导出
3.新增暗黑模式
5.一级分类新增颜色，用于区分
6.新建用户以及智能体待办功能

修复一些显示bug
